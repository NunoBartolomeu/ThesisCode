import { Layout } from '@/components/ui/Layout';
import { LedgersList } from '@/components/ledger/LedgersList';

export default function LedgersPage() {
  return (
    <Layout>
      <LedgersList />
    </Layout>
  );
}